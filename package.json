{
  "name": "whatsapp-web-js-server",
  "version": "2.0.0",
  "description": "WhatsApp Web.js server for real-time integration",
  "main": "server-web.js",
  "scripts": {
    "start": "node server-web.js",
    "dev": "nodemon server-web.js",
    "build": "echo 'Build completed'",
    "start:baileys": "node server.js",
    "start:test": "node test-server.js",
    "verify-chromium": "node -e \"const puppeteerConfig = require('./.puppeteerrc.cjs'); console.log('Puppeteer will use:', puppeteerConfig.executablePath);\"",
    "postinstall": "npm run verify-chromium"
  },
  "dependencies": {
    "axios": "^1.7.7",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "puppeteer": "^24.16.0",
    "qrcode": "^1.5.3",
    "whatsapp-web.js": "^1.31.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
